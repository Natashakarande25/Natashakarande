A. Implement simple linear regression using a dataset
we are using the dataset “marketing.csv”

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_squared_error, r2_score
import seaborn as sns
import warnings
from sklearn.model_selection import train_test_split


df = pd.read_csv("marketing.csv")
df.head()


df.shape

df.isnull().sum

df.dtypes

df.info

plt.figure(figsize=(10,5))
sns.pairplot(data=df, y_vars=['sales'], x_vars=['youtube', 'facebook', 'newspaper'])


x = df[['newspaper']] 
y = df[['sales']] 
x_train , x_test , y_train , y_test = train_test_split(x,y , test_size = 0.2 , random_state = 42)
model = LinearRegression()
model.fit(x_train , y_train)


B. Explore and interpret the regression model coefficients and goodness-offit measures.

y_test_pred = model.predict(x_test)
mse = mean_squared_error(y_test, y_test_pred)
print(mse)
r2_test = r2_score(y_test, y_test_pred)
print(r2_test)
print("Intercept : ", model.intercept_)
print("Slope (Coefficient): ", model.coef_[0])

C. Extend the analysis to multiple linear regression and assess the impact
of additional predictors

x = df[['youtube']] 
y = df[['sales']]   
x_train, x_test, y_train, y_test = train_test_split(x, y, test_size = 0.2, random_state = 42)
model = LinearRegression()
model.fit(x_train, y_train)

y_test_pred = model.predict(x_test)
mse = mean_squared_error(y_test, y_test_pred)
print(mse)
r2_test = r2_score(y_test, y_test_pred)
print(r2_test)
print("Intercept : ", model.intercept_)
print("Slope (Coefficient): ", model.coef_[0])


x = df[['facebook']] 
y = df[['sales']]    
x_train, x_test, y_train, y_test = train_test_split(x, y, test_size = 0.2, random_state = 42)
model = LinearRegression()
model.fit(x_train, y_train)


y_test_pred = model.predict(x_test)
mse = mean_squared_error(y_test, y_test_pred)
print(mse)
r2_test = r2_score(y_test, y_test_pred)
print(r2_test)
print("Intercept : ", model.intercept_)
print("Slope (Coefficient): ", model.coef_[0])


x = df[['youtube', 'facebook']] 
y = df['sales']
x_train , x_test , y_train , y_test = train_test_split(x,y , test_size = 0.2 , random_state = 42)
model = LinearRegression()
model.fit(x_train , y_train)
y_test_pred = model.predict(x_test)
mse = mean_squared_error(y_test, y_test_pred)
print(mse)
r2_test = r2_score(y_test , y_test_pred )
print(r2_test)
print("Intercept : " , model.intercept_)
print("Coefficients :")
print("Youtube: ", model.coef_[0])
print("Facebook:", model.coef_[1])
n_test = x_test.shape[0]
p = x_test.shape[1]
adj_r2 = 1 - ( 1 - r2_test ) * (n_test - 1) / (n_test - p - 1)
print(adj_r2)
